<h1>Add a dose</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Add a dose", class: "btn btn-dark" %>
  <%= link_to "Cancel", cocktail_path(@cocktail), class: "btn btn-secondary" %>
<% end %>
